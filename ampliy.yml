version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-helpdesk && npm install
    build:
      commands:
        - cd my-helpdesk && npm run build
  artifacts:
    baseDirectory: my-helpdesk/.next
    files:
      - '**/*'
  cache:
    paths:
      - my-helpdesk/node_modules/**/*